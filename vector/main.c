#include <stdio.h>
#include <stdlib.h>

#include <math.h>

#include "types.h"
#include "vector.h"

int main(void) {
/*
    printf("%lf\n", hypot(3.0, 4.0));
    size_t n;
    scanf("%zu", &n);

    Vector vector;
    if (vectorCreate(&vector, n) != 0) {
        perror("vectorCreate");
        exit(EXIT_FAILURE);
    }

    for (size_t i = 0; i < vector.size; ++i)
        scanf("%lf", vector.ptr + i);

    printf("capacity = %zu, size = %zu\n", vector.capacity, vector.size);
    for (size_t i = 0; i < vector.size; ++i)
        printf("%lf ", vector.ptr[i]);
    printf("\n");

    dbl value = 1.0;
    while (value != 0.0) {
        scanf("%lf", &value);
        vectorPushBack(&vector, value);
        printf("capacity = %zu, size = %zu\n", vector.capacity, vector.size);
        for (size_t i = 0; i < vector.size; ++i)
            printf("%lf ", vector.ptr[i]);
        printf("\n");
    }

    vectorDestroy(&vector);
*/
    return 0;
}

